
name: Node CI
 
on: [push]
 
jobs:
  build:
 
    runs-on: ubuntu-latest
    strategy:
          matrix:
            node-version: [16]
    steps:
#     - uses: actions/checkout@master
    - name: Install
      uses: actions/setup-node@v3
    - name: test
      run: |
        npm i
        npm run dev
